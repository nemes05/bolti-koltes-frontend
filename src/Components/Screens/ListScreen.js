import { View } from 'react-native'
import { Text } from 'react-native-paper'

const ListScreen = () => {
    return (
        <>
            <View
                style={{
                    alignItems: 'center',
                    height: 100,
                    padding: 20,
                }}
            >
                <Text>Lista</Text>
            </View>
        </>
    )
}

export default ListScreen
