import { View } from 'react-native'
import { Text } from 'react-native-paper'

const CartScreen = () => {
    return (
        <View
            style={{
                alignItems: 'center',
                height: 100,
                padding: 20,
            }}
        >
            <Text>Cart</Text>
        </View>
    )
}

export default CartScreen
